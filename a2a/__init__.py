"""Arch2AppimageGUI — search prebuilt Arch binary repos and build AppImages.

Runs on any Linux distro: packages are fetched straight from pacman mirrors as
``.pkg.tar.zst`` and assembled into a self-contained AppImage. No pacman needed,
no building from source.
"""

__version__ = "0.1.0"
